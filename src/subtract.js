// function subtract(a, b) {
//   return a - b;
// }

/**
 * subtract function
 *
 * takes two values and subtracts the first from the second
 *
 * @param {Number} a
 * @param {Number} b
 *
 * @returns Number
 */
const subtract = (a, b) => {
  return a - b;
};

export default subtract;
