/**
 * add function
 *
 * takes two values and returns true if the values are the same
 *
 * @param {Number} a
 * @param {Number} b
 *
 * @returns Number
 */

export default function add(a, b) {
  return a + b;
}
