/**
 * divide function
 *
 * takes two values and divides the first by the second
 *
 * @param {Number} a
 * @param {Number} b
 *
 * @returns Number
 */

function divide(a, b) {
  return a / b;
}
export default divide;
