/**
 * multiply function
 *
 * takes two values and multiplies the first by the second
 *
 * @param {Number} a
 * @param {Number} b
 *
 * @returns Number
 */

const multiply = (a, b) => {
  return a * b
}

export default multiply
